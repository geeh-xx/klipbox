<p>Se você ja possui um codigo de usuario basta digitar no campo logo abaixo, mas caso não tenha, realize<br />
um <%= link_to 'cadastro', new_user_path%></p>
<br />

<%= form_tag(users_path, :method => :get) do %>
  <%= text_field_tag 'search', nil, placeholder: 'Digite o codigo do usuario' %>
  <%= submit_tag "Pesquisar " %>
<% end %>
<br />

<% @users.each do |user| %>
    <div class="row">
      <div class="col s12 m6">
        <div class="card blue-grey darken-1">
          <div class="card-content white-text">
            <span class="card-title">Codigo: <%= user.id %></span>
            <p>Nome: <%= user.name %></p>
            <p>Email: <%= user.email %></p>
            <p>Palavra: <%= user.word %></p>
          </div>
          <div class="card-action">
            <%=  link_to 'Editar Usuario', edit_user_path(user) %>

            <%=  link_to 'Deletar Usuario', user, method: :delete,data: { confirm: "Deseja realmente deletar este usuario?" } %>
          </div>
        </div>
      </div>
    </div>
<% end -%>


<div class="fixed-action-btn" style="bottom: 45px; right: 24px;">
  <a class="btn-floating btn-large red">
    <i class="large material-icons">mode_edit</i>
  </a>
  <ul>
    <li>
      <%= link_to  users_path, class:"btn-floating btn-large red"  do%>
          <i class="material-icons">search</i>
      <%end %>
    </li>

    <li>
      <%= link_to  new_user_path, class:"btn-floating btn-large red"  do%>
        <i class="material-icons">add</i>
      <%end %>
    </li>
  </ul>

</div>


